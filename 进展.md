# 项目进展

## 任务包 C：游戏规则（已完成）

**完成时间**：2026-08-19
**负责人**：任务包 C agent
**状态**：已完成

### 产出文件
- `D:\temp\gpt投篮\js\score-system.js` — 计分系统
- `D:\temp\gpt投篮\js\combo.js` — 连击系统
- `D:\temp\gpt投篮\js\timer.js` — 倒计时
- `D:\temp\gpt投篮\js\game-over.js` — 结算与游戏结束
- `D:\temp\gpt投篮\js\restart.js` — 重新开始
- `D:\temp\gpt投篮\js\game-rules.js` — 游戏规则核心（状态机）

### 关键约定
- 所有模块挂在 `window.<Name>System` / `window.GameRules` 全局命名空间下
- 中文注释；纯逻辑层，只改 gameState，不操作 DOM
- 通过 `gameState.shotResolved` 保证一次投篮只结算一次
- 暂停时（phase === 'PAUSED'）不推进计时与判定
- 三分线半径 300px，普通命中 +2，Perfect 中心偏差 ≤12px +1，Swish 未碰边缘 +1
- Combo 上限 10，命中 +1、Miss 归零
- 计时 30 秒，最后 5 秒红色闪烁（0.5s 周期）

### 验证
- 全部 6 个文件通过 `node --check` 语法校验
- 与 B 包已有文件（feedback.js / net-animation.js / particles.js / screen-shake.js / trajectory.js）无命名冲突
- 与 A 包 `config.js` 的 `GAME_CONFIG` 对齐（perfectThreshold=12, normal=2, swishBonus=1 等）

### 依赖关系
- `GameRules.updateGameState` → 顺序调用 `TimerSystem.updateTimer` → `ComboSystem.updateComboAnimation` → `GameRules.checkShotResult` → `GameOverSystem.handleGameOver`
- 投篮出手需在外部 `shot.js` 调用 `GameRules.beginShot(gameState, shootFrom)` 记录出手点（用于三分判定）
