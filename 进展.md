# gpt投篮 项目契约

> 本文档是 gpt投篮 项目的权威契约文件，记录团队协作约定、项目全貌、部署信息和进展。

---

## 📜 契约约定

### 约定 1：所有 session 必须使用 superpowers 插件
> 不论主 session 还是后台子代理，**开始任何开发动作前必须调用 `Skill` 工具加载 superpowers**。
> 涵盖：brainstorming、writing-plans、test-driven-development、verification-before-completion 等。
> 禁止跳过硬性流程：写代码前要写计划、改代码前要写测试、声称完成前要验证。

### 约定 2：部署信息必须记入本文档
> 每次部署变更后，**必须更新本文档的"部署信息"章节**。
> 包括：服务器 IP、端口、URL、部署路径、部署方式、上次部署时间。
> 任何 session 都可以从这里查到最新的部署状态。

### 约定 3：用户角色
- 用户是**非技术 PM**
- 商业完整性由用户判定，技术细节由 Claude 决定
- 询问用户时只问**业务问题**，不问技术问题

### 约定 4：权限边界
- Claude 可**自动执行**：git、npm、ssh、curl、文件读写
- 必须**询问**用户：删除/覆盖文件、对外发送内容、部署到生产

---

## 📋 项目全貌

### 项目定义
**gpt投篮** 是一款基于 Web Canvas 2D 的休闲投篮小游戏，30 秒内尽可能多地命中投篮。

### 核心设计原则
- **真实感 + 可控性 + 游戏性** > 完全真实的物理模拟
- 简化空气阻力、旋转、碰撞、篮板、人物动作
- 保证：轨迹合理、结果可预测、操作有反馈

### 技术栈
- **纯前端**：HTML5 + CSS3 + JavaScript (ES6+) + Canvas 2D
- **无后端**：所有逻辑在客户端运行
- **响应式**：支持 16:9 / 4:3 / 9:16，desktop + mobile
- **触控**：Pointer Events（统一 mouse/touch/pen）

### 默认游戏模式
- 模式：**限时连续投篮**
- 时长：30 秒
- 得分：普通命中 +2，Perfect +1，Swish +1
- Combo：连续命中 +1（上限 10），Miss 归零

---

## 🚀 部署信息

### 云服务器
| 项目 | 值 |
|------|---|
| IP | `124.223.99.237` |
| 用户 | `root` |
| SSH 密钥 | `C:\Users\gong'cheng\.ssh\id_ed25519` |
| 部署路径 | `/opt/gpt-投篮/` |
| 符号链接 | `/usr/share/nginx/html/gpt-basketball/` → `/opt/gpt-投篮/` |
| 访问端口 | `8666` |
| 访问 URL | `http://124.223.99.237:8666/gpt-basketball/` |
| 部署方式 | **GitHub + git bundle**（服务器无法直连 GitHub） |

### GitHub 仓库
- 地址：`git@github.com:poorcid858-prog/GPT-.git`
- 分支：`master`
- remote 已配置：`origin`

### nginx 配置
- 配置文件：`/etc/nginx/conf.d/ai-assistant-8666.conf`
- 端口：`8666`
- 路由规则：
  - `/ai/` → proxy 到本地 3000 端口（ai-assistant）
  - `/gpt-basketball/` → 静态文件（符号链接到 `/opt/gpt-投篮/`）
  - `/` → 404

### 部署流程（git bundle 离线传输）
1. 本地：`git bundle create gpt-shoot.bundle master`
2. 上传：`scp gpt-shoot.bundle root@124.223.99.237:/opt/gpt-投篮/bundle.git`
3. 服务器：`git fetch /opt/gpt-投篮/bundle.git master:refs/remotes/origin/master`
4. 服务器：`git checkout -b master origin/master`
5. 创建符号链接：`ln -sfn /opt/gpt-投篮 /usr/share/nginx/html/gpt-basketball`
6. Reload nginx：`nginx -s reload`

### 历史部署记录
| 时间 | 版本 | 描述 | 操作人 |
|------|------|------|--------|
| 2026-08-20 00:45 | de4b43b | MVP 核心玩法闭环 | Claude + PM |

---

## 📅 进展记录

### 2026-08-19 ~ 2026-08-20
- ✅ 任务包 A：骨架（index.html / config.js / game-state.js / game-loop.js / game.js）
- ✅ 任务包 B：核心玩法（physics / ball / rim / backboard / collision / scoring / shot）
- ✅ 任务包 C：游戏规则（score-system / combo / timer / game-over / restart / game-rules）
- ✅ 任务包 D：瞄准反馈（aim-guide / trajectory / particles / feedback / screen-shake / net-animation / sound-manager）
- ✅ 集成 glue code（game.js 中 registerModuleGlue 注入 7 updates + 7 renders + 1 input）
- ✅ Smoke test 验证（23 个 JS 文件 0 错误，15 个钩子 0 错误）
- ✅ 项目契约文档创建（本文档）
- ✅ GitHub 仓库配置 + 代码推送（commit de4b43b）
- ✅ 云服务器部署（git bundle 传输 + nginx 配置）
- ✅ 部署验证（HTTP 200，3 个关键文件测试通过）
- ✅ **系统性修复**：背景图、人物渲染、篮球飞行消失问题（2026-08-20）

### 文件清单
```
gpt投篮/
├── index.html          (7.0 KB)  游戏入口
├── config.js           (1.9 KB)  集中配置
├── game-state.js       (5.0 KB)  状态机
├── game-loop.js        (3.7 KB)  游戏循环
├── game.js             (20.4 KB) 主入口 + 集成 glue code
├── package.json        (248 B)
├── 进展.md             (本文件)
├── js/
│   ├── physics.js      (5.2 KB)  物理引擎
│   ├── ball.js         (4.9 KB)  篮球实体
│   ├── rim.js          (10.1 KB) 篮筐实体
│   ├── backboard.js    (2.7 KB)  篮板
│   ├── collision.js    (6.7 KB)  碰撞检测
│   ├── scoring.js      (5.2 KB)  进球判定
│   ├── shot.js         (3.7 KB)  投篮触发
│   ├── score-system.js (5.5 KB)  得分系统
│   ├── combo.js        (4.2 KB)  连击系统
│   ├── timer.js        (6.9 KB)  计时系统
│   ├── game-over.js    (5.8 KB)  结算面板
│   ├── restart.js      (6.4 KB)  重开功能
│   ├── game-rules.js   (17.7 KB) 规则整合
│   ├── aim-guide.js    (4.8 KB)  瞄准线
│   ├── trajectory.js   (5.6 KB)  轨迹预测
│   ├── particles.js    (4.7 KB)  粒子系统
│   ├── feedback.js     (5.8 KB)  视觉反馈
│   ├── screen-shake.js (3.4 KB)  屏幕震动
│   ├── net-animation.js(5.5 KB)  篮网动画
│   └── sound-manager.js(8.0 KB)  音效管理
└── assets/             已有素材
    ├── images/         篮球/篮筐/篮板/背景
    ├── player/         射手动画 6 帧
    └── sounds/         7 个音效 WAV
```

### 待完成
- [ ] 任务包 E：UI 界面 + 响应式 + 页面完善
- [ ] 任务包 F：难度系统 + 模式扩展 + 验收
- [ ] 浏览器端真实测试（需要用户打开 URL 验证）

---

## 🔧 系统性修复记录（2026-08-20）

### 问题 1：人物看不见
**根因**：代码中没有任何地方加载或绘制 `assets/player/` 下的图片
**修复**：
- 在 `game.js` 中添加 `loadPlayerImage()` 函数，加载 `player.png`
- 在 `registerModuleGlue()` 中添加人物渲染钩子，在篮球初始位置附近绘制球员
- 如果人物图片加载失败，绘制简笔人物作为 fallback

### 问题 2：没有背景图
**根因**：`clearStage()` 函数只用纯色填充背景，没有加载背景图片
**修复**：
- 在 `game.js` 中添加 `loadBackgroundImage()` 函数，优先加载 `arena-background.png`，失败则尝试 `court-background.png`
- 修改 `clearStage()` 函数，优先绘制背景图片，如果加载失败则使用纯色背景

### 问题 3：松开鼠标后篮球不见了
**根因**：
1. `scoring.js` 的 `onScore` 和 `onMiss` 函数中，设置了 `ball.shotResolved = true`，但没有设置 `gameState.currentShot.resolved = true`，导致游戏状态无法从 `BALL_FLYING` 流转到 `SCORED` 或 `MISSED`
2. 物理参数 `baseForce = 900` 太大，导致篮球飞出画布
3. 边界判定 `isBallOutOfBounds` 的边界容差不够宽松

**修复**：
- 在 `scoring.js` 的 `onScore` 和 `onMiss` 函数中，添加设置 `gameState.currentShot.resolved = true` 的代码
- 在 `config.js` 中，将 `baseForce` 从 900 降低到 700
- 在 `trajectory.js` 中，将 `TRAJECTORY_BASE_FORCE` 从 900 降低到 700
- 在 `physics.js` 中，调整基础力为 700
- 在 `game.js` 中，调整 `isBallOutOfBounds` 函数的边界容差，增加飞行超时到 4 秒，边界容差到 150 像素

### 修改文件清单
| 文件 | 修改内容 |
|------|----------|
| `game.js` | 添加背景/人物加载函数，修改 clearStage()，添加人物渲染钩子，调整边界判定 |
| `config.js` | 将 baseForce 从 900 降低到 700 |
| `js/physics.js` | 调整基础力为 700 |
| `js/scoring.js` | 在 onScore 和 onMiss 中设置 gameState.currentShot.resolved = true |
| `js/trajectory.js` | 将 TRAJECTORY_BASE_FORCE 从 900 降低到 700 |
| `进展.md` | 添加修复记录 |

### 修复后预期效果
1. **背景图可见**：游戏启动后显示 arena-background.png 或 court-background.png 作为背景
2. **人物可见**：在篮球初始位置附近显示球员（优先使用 PNG，失败则绘制简笔人物）
3. **篮球飞行正常**：松开鼠标后篮球正常飞行，不会消失，能正确进球或 Miss
